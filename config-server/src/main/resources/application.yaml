server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/faridahmad89/ecommerce-system.git
          search-paths: config-repo
          default-label: main